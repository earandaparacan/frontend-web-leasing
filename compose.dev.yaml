services:
  web:
    build:
      target: development
    environment:
      WATCHPACK_POLLING: "true"
    volumes:
      - .:/app
      - web_node_modules:/app/node_modules
      - web_next_cache:/app/.next

volumes:
  web_node_modules:
  web_next_cache:
